describe('Basic Test Suite', () => {
  it('should pass a simple truthy test', () => {
    expect(true).toBe(true);
  });

  it('should calculate basic math correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
